import { Card } from '@/components/ui/Card';

export function RegisterPage() {
  return (
    <Card className="mx-auto max-w-xl">
      <p className="text-sm uppercase tracking-[0.28em] text-text-muted">Register placeholder</p>
      <h2 className="mt-3 text-2xl font-semibold text-text">Registration arrives in Chunk 2</h2>
      <p className="mt-3 text-sm leading-6 text-text-muted">
        This placeholder keeps the router complete until the production auth form is built.
      </p>
    </Card>
  );
}